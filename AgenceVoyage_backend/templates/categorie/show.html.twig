{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ categorie.pays }}</td>
            </tr>
            <tr>
                <th>Evenement</th>
                <td>{{ categorie.evenement }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ categorie.duree ? categorie.duree|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_index') }}">back to list</a>

    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
